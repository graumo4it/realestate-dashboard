# План B — Claude Code (внедрение в кодовую базу)

> Цель: перенести утверждённую дизайн-систему (из Плана A / `DESIGN_SPEC.md`) в реальный
> фронтенд минимально-инвазивно. Все 50 HTML тянут `variables.css`/`main.css` — работаем
> через токены, чтобы не править каждый файл руками.
>
> Зависимость: стартует после утверждения `DESIGN_SPEC.md` (особенно палитра серий и hero).
> C1+C2 можно начать параллельно с Планом A — это инфраструктура.
>
> Метки: 🆕 — добавлено по итогам живого визуального аудита; ✏️ — изменён приоритет.

## C1. Токены и темизация *(первым)*
- `css/variables.css`: семантические токены + два набора через `[data-theme="dark"]` на `<html>`.
  Старые имена переменных — как алиасы, чтобы ничего не сломать.
- 🆕 `--series-1…6` из утверждённой палитры + JS-зеркало `SERIES_PALETTE` в `js/utils.js`.
- Переключатель темы в шапку + anti-FOUC inline-скрипт (localStorage + `prefers-color-scheme`,
  ставится до рендера). Подключить во все HTML (sed-патч одинакового блока шапки).

## C2. Единая тема ECharts
- Вынести повторяющийся `option` из `chart-page.js`, `combo-page.js`, `subsidy-purpose-page.js`
  в `js/echarts-theme.js`: единые grid/axis/tooltip/dataZoom/textStyle, чтение цветов из
  CSS-переменных (`getComputedStyle`), пересборка темы при смене dark/light.
- Заменить хардкод `#C8181A` / `#DDE2E8` / `#7A8B9A`.
- 🆕 Комбо-серии — строго из `SERIES_PALETTE` (убрать фиолетовый); легенда-точки из той же палитры.
- 🆕 Перерисовать `dataZoom` handle-стиль (тонкий), `chart-page.js:350`.
- 🆕 Поправить плотность `areaStyle` по решению из D2, `chart-page.js:422`.

## C3. Улучшения графиков
- 🆕 `series.endLabel` (direct-label последней точки) + опц. `markPoint` на макс/мин.
- Единицы измерения — в подпись оси Y, не в каждую метку.
- Тач-тултип / `triggerOn` для мобильных; адаптивная высота `#main-chart`.

## C4. Шапка / hero / футер ✏️ *(сразу после C1)*
- 🆕 **Выпилить серый `#3F4C50`** из `.site-header` / `.hero` / `.site-footer` → токены светлой схемы.
- 🆕 Поправить контраст `.hero-eyebrow` и `.chart-meta` (a11y AA), `main.css:105`.
- Зигзаг/стрелку бренда — как CSS-фон (SVG data-uri, `--accent-2`, low-opacity, reduced-motion safe).

## C5. Иконки и карточки разделов 🆕
- Заменить эмодзи в `CATEGORY_ICONS` (`index.html:80`) на инлайн-SVG спрайт.
- Наполнить `.category-card` спарклайном/цифрой (переиспользовать `sparkline.js`; данные уже
  грузятся для счётчиков видимых карточек).

## C6. Спарклайны и адаптив 🆕
- Унифицировать `sparkline.js` — единый вид (area везде или нигде).
- Починить навигацию `category` <900px (`chart.css:295`) по решению из D3.
- Сделать тоггл-серии в таблице визуально-кликабельными (`category.html`).

## C7. Доступность и состояния
- `:focus-visible` для кнопок/ссылок/`.filter-btn`/`.btn-group`; `aria` на тулбарах;
  контраст muted до WCAG AA.
- 🆕 Состояния **empty/error** в `chart-page.js` и `category` (сейчас только skeleton).

## C8. Полировка таблиц/KPI
- Дельта-стрелка-глиф в `.delta` (`main.css`); `font-variant-numeric: tabular-nums` в числовых колонках.
- ✏️ Уплотнить/дополнить KPI-плашку по макету D3.

## Порядок и проверка
**C1 → C4 → C2 → C5 → C3 → C6 → C7 → C8.**
Визуальная проверка через preview-воркфлоу (backend :8001 + frontend) после C4 и C2:
скриншоты light/dark на `index`, `chart`, одной комбо-странице. Ветка `v1.2-improvements`,
коммитить поэтапно.
