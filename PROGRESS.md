# PROGRESS.md — Трекер разработки
## Дашборд «Статистика рынка жилой недвижимости России»

> Последнее обновление: 10 мая 2026
> Ветка разработки: `v1.2-improvements`
> Backend: порт **8001** | Frontend: порт **3000**
> Путь: `/Users/egor/Работа/data hub/realestate-dashboard/`

---

## Быстрый запуск

```bash
# Backend
cd /Users/egor/Работа/data\ hub/realestate-dashboard/backend
uvicorn app.main:app --reload --port 8001

# Frontend (фоновый режим)
cd /Users/egor/Работа/data\ hub/realestate-dashboard/frontend
nohup python3 -m http.server 3000 > /tmp/frontend.log 2>&1 &

# Остановить frontend
lsof -ti:3000 | xargs kill -9

# Python окружение (venv)
source venv/bin/activate
```

---

## Статус по исходному плану ТЗ

| # | Задача | Статус | Файлы |
|---|--------|--------|-------|
| 1 | Структура репозитория, .gitignore, README | ✅ | `.gitignore`, `README.md` |
| 2 | Схема БД (DDL + materialized view) | ✅ | `migration/001_init.sql` |
| 3 | Скрипт миграции Excel → БД | ✅ | `migration/excel_to_db.py` |
| 4 | FastAPI backend (все эндпоинты) | ✅ | `backend/app/` |
| 5 | Проверка API | ✅ | — |
| 6 | Frontend: index.html + chart.html | ✅ | `frontend/index.html`, `frontend/chart.html` |
| 7 | Frontend: category.html | ✅ | `frontend/category.html` |
| 8 | Парсер ЦБ РФ | ✅ | `parsers/cbr.py` |
| 9 | Парсеры ДОМ.РФ, Росстат | ✅ | `parsers/domrf.py`, `parsers/rosstat.py` |
| 10 | GitHub Actions (CI + расписание парсеров) | ✅ | `.github/workflows/` |
| 11 | Продакшн (VPS + Docker) | ⏳ | `docker-compose.prod.yml` |
| 12 | Автодеплой при push в main | ✅ | `.github/workflows/deploy.yml` |

---

## Блок правок v1.1

### Блок 1 — Улучшения chart-page ✅

| Задача | Файл |
|--------|------|
| MoM в backend (п.п. для %, % для остальных) | `services/data_service.py` |
| MoM во frontend | `js/chart-page.js` |
| Кнопки ретроспективы 1г/3г/5л — от последней точки данных | `js/chart-page.js` |
| `cleanName()` — очистка названий от кодов | `js/utils.js` |
| Кнопка «За всё время» | `js/chart-page.js` |
| Объединены плашки «Последнее значение» и «Период» | `chart.html` |
| Ползунок временной шкалы (ECharts dataZoom) | `js/chart-page.js` |
| Панель фильтров в сайдбаре | `chart.html`, `css/chart.css` |
| Исправлены единицы измерения ~20 индикаторов | SQL в БД |
| Скрыты: 5.4, разделы Домклик/Frank RG, ряд 6.xx | SQL `is_public=false` |
| Типы графиков: 5.20, 5.8 → bar | SQL `chart_type='bar'` |
| п.п. для процентных, xData из `p.label`, кв./кв. для quarterly | `js/chart-page.js` |
| Заголовок таблицы: «Значение, {unit}» | `js/chart-page.js` |

### Блок 2 — Данные из fedstat.ru ✅

Скрипт: `migration/fetch_fedstat.py`
⚠️ Запускать только локально — fedstat блокирует облачные IP

| Код | Индикатор | Точек | Период |
|-----|-----------|-------|--------|
| `2.9` | Жилфонд | 18 | 2008–2025 |
| `2.11` | Прибыло жилфонда | 17 | 2008–2025 |
| `2.12` | Выбыло жилфонда | 18 | 2008–2025 |
| `2.13.ext` | Доля ветхого жилья | 10 | 2006–2015 |
| `1.2` | Среднедушевые доходы (quarterly) | 56 | 2012–2025 |
| `1.3` | Зарплата (quarterly) | 35 | 2017–2025 |
| `1.1` | Население | 36 | 1990–2025, захардкожено + парсер fedstat/31557 |

### Блок 3 — Расчётные индикаторы ⏳

| Код | Формула | Статус |
|-----|---------|--------|
| `2.10` Обеспеченность жильём на конец года | жилфонд[N] / население[N+1] | ✅ |
| `5.9` Темп продаж | 5.8 / 3.1 × 100% | ⏳ |
| `5.10` КДЖ | цена 54 кв.м / доход семьи | ⏳ |
| `5.11` Доступность по ипотеке | платёж < 50% дохода | ⏳ |
| `5.12–5.15` Потребность в жилье | методика ДОМ.РФ | ⏳ |

### Блок 4 — Комбо-страницы ✅

Все 20 файлов созданы в `frontend/`. Навигационная увязка — **Блок 6 (текущая задача)**.

| Файл | Индикаторы | Тип графика |
|------|-----------|-------------|
| `combo-chart.html` | 2.2, 2.3 | bar stacked (МЖС+ИЖС) |
| `share-chart.html` | 2.2, 2.3 | bar 100% (структура %) |
| `ihh-chart.html` | 3.18, 3.19 | line (ИХХ средний+медианный) |
| `prices-chart.html` | 4.4.x, 4.5.x | line, двухуровневые вкладки |
| `mortgage-count.html` | 6.7, 6.13, 6.1 | bar stacked |
| `mortgage-volume.html` | 6.8, 6.14, 6.2 | bar stacked |
| `mortgage-rate.html` | 6.3, 6.9, 6.15 | line, п.п. |
| `mortgage-term.html` | 6.4, 6.10, 6.16 | line |
| `mortgage-size.html` | 6.5, 6.11, 6.17 | line |
| `mortgage-payment.html` | 6.6, 6.12, 6.18 | line |
| `subsidy-count.html` | 6.36, 6.38, 6.40, 6.42, 6.44 | bar stacked |
| `subsidy-volume.html` | 6.37, 6.39, 6.41, 6.43, 6.45 | bar stacked |
| `subsidy-ddu-count.html` | 6.46, 6.48, 6.50, 6.52 | bar stacked |
| `subsidy-ddu-volume.html` | 6.47, 6.49, 6.51, 6.53 | bar stacked |
| `igs-count.html` | 6.70, 6.76, 6.82 | bar stacked |
| `igs-volume.html` | 6.71, 6.77, 6.83 | bar stacked |
| `igs-rate.html` | 6.72, 6.78, 6.84 | line, п.п. |
| `igs-term.html` | 6.73, 6.79, 6.85 | line |
| `igs-size.html` | 6.74, 6.80, 6.86 | line |
| `igs-payment.html` | 6.75, 6.81, 6.87 | line |

### Блок 5 — Форматирование данных в БД ✅

Применено в локальной БД. Дополнительные миграции:
- `migration/migrate_prices_main.py` — 4.4 и 4.5 как квартальные ✅
- `migration/migrate_prices_subtypes.py` — 4.4.1–4.5.4 по типам жилья ✅

### Блок 6 — Увязка навигации ✅

**Проблема:** `category.html` показывает все индикаторы как отдельные карточки → `chart.html?code=X`. Комбо-страницы изолированы.

**Решение:** `COMBO_OVERRIDES` — JS-константа с тремя режимами для каждой записи:
- `parentCode` — заменить карточку этого индикатора ссылкой на combo URL
- `hideCodes` — скрыть эти индикаторы из списка
- `parentCode: null` — добавить новую карточку (комбо без родителя)

---

## Блок правок v1.6 — 8–9 мая 2026

### БД — переименования и правки ✅

Скрипт: `migration/rename_fixes.sql`

- Переименованы 53 индикатора и 2 раздела через SQL UPDATE
- `supply_volume` → «Ввод жилья», `concentration` → «Уровень концентрации рынка»
- Жилфонд: 2.11→«Прибыло жилфонда», 2.12→«Выбыло жилфонда», 2.13→краткое
- ДОМ.РФ строящееся жильё: все uc_* переименованы по реальным кодам БД
- Спрос: 5.1, 5.8, 5.9, 5.10, 5.11, 5.13, 5.15, 5.20, 5.21, 5.3, apt_area
- Ипотека: 6.3, 6.4, 6.5, 6.6, 6.7, 6.8, 6.19, 6.20, 6.21, 6.70–6.75
- Цены: 4.1, 4.4, 4.8, 4.9; apt_budget перенесён в раздел «Цены»
- Удалены дубли: «Средняя цена 1 кв. м машиноместа» и «Средний бюджет машиноместа»
- 5.1 chart_type → bar

### Frontend — UX и логика ✅

**`js/utils.js`:**
- `fmtDate()` для monthly → двустрочный формат `янв.\n2024` для осей
- Добавлена `fmtDateInline()` — однострочный вариант для KPI и таблиц
- ё → е везде

**`js/chart-page.js`:**
- Ось X: `rotate: 0` (всегда горизонтально), автоматический `interval`
- Ось Y: знак `−` для отрицательных, `%` если единица `%`, разделитель разрядов
- Tooltip: `Значение: 3 200` — единица только в значении, не в названии серии
- Таблица: заголовки `ИЗМ. Г/Г` / `ИЗМ. М/М`, значения динамики с единицами
- Breadcrumb: показывает название индикатора вместо кода
- Квартальные данные: `interval: 3` (только Q1 каждого года)
- ё → е везде

**`chart.html`:**
- Кнопки: `1 год / 3 года / 5 лет / За все время`
- Сайдбар убран, контент на всю ширину (`display: block`)
- ё → е везде

**`index.html`:**
- Скрыты карточки `mortgage_total` и `mortgage_secondary`
- Счётчики показателей исправлены через `VISIBLE_COUNT_OVERRIDE`
- Nav в header: хардкодированные короткие названия вместо API
- Ссылка «Ипотека» → `mortgage_base`

**`category.html`:**
- Nav: хардкодированные короткие названия
- Счётчик индикаторов в подзаголовке — корректный

### Комбо-страницы — горизонтальная панель фильтров ✅

Сайдбар убран на всех комбо-страницах. Добавлена горизонтальная панель:
- Кнопка **«Тип индикатора»** с дропдауном (чекбоксы серий)
- Кнопка **«Сбросить»** — восстанавливает все серии
- Кнопка темнеет когда не все серии активны

Затронутые файлы (25 штук):
- `mortgage-count/volume/rate/term/size/payment.html`
- `subsidy-count/volume.html`
- `igs-count/volume/rate/term/size/payment.html`
- `combo-chart/share-chart/ihh-chart.html`
- `uc-area/new/new-vs-input/stock/new-vs-sales/absorption.html`
- `debt-volume/overdue/overdue-share.html`
- `prices-chart.html` — сайдбар убран, панель не добавлена (фильтрация через вкладки)

**Исправления KPI-блоков:**
- `mortgage-rate.html`: KPI период из `<span>` → `div.kpi-period`, дельты через `deltaHtml(v, true)`
- `mortgage-volume.html`: добавлена плашка «Всего», убрана дублирующая кнопка «Данные за год»
- `subsidy-count/volume.html`: убрана KPI-плашка «Льготная» (программа завершилась)
- `mortgage-payment/term/size.html`: убраны дублирующие кнопки «Данные за год»
- Все 13 igs/subsidy файлов: убраны дублирующие вызовы `injectAnnualBtn`

**Стили таблиц:**
- Кнопки-табы: `flex: 1; text-align: center` — полноширинные как в `mortgage-count`

### Прочие правки ✅

- Ось X на всех страницах: `rotate: 0` (заменено `rotate: 30` → `rotate: 0`)
- `mortgage-rate.html`: ось Y без десятичных знаков, кнопки тулбара без «п.п.»
- `subsidy-count/volume.html`: кнопка «Льготная» убрана из KPI, но осталась в графике, легенде и таблице
- Nav унифицирован на всех страницах через хардкод

---

## Блок правок v1.7 — 10 мая 2026

### Фикс дропдауна серий на debt-volume / debt-overdue ✅

**Проблема 1 — `ReferenceError: Can't find variable: KEYS`**
В обоих файлах константа объявлена как `CHART_KEYS`, но в нескольких местах код обращался к несуществующему `KEYS` → `buildDropdown()` падал при загрузке, графики и KPI не рендерились.

В `debt-volume.html` дополнительно был дублирующий блок дропдауна (старая ES6-версия, ~80 строк) — его function-declarations перетирались более поздним блоком, но top-level `addEventListener`-ы навешивались и валились бы при кликах.

**Проблема 2 — «галочка не пропадает в окошке, при этом график меняется»**
Обработчик `dropdown.addEventListener('click', ...)` содержал ручную инверсию:
```js
if (e.target !== cb) cb.checked = !cb.checked;
```
При клике на текст / точку-индикатор / край строки один и тот же click-обработчик ловил событие дважды (от потомка и от родительского `<div>` через bubbling) — первый раз корректно снимал галочку, второй возвращал. Воспроизведено под jsdom: вторая сработка визуально маскирует первую, поэтому казалось, что «галочка осталась, но график изменился».

**Решение:**
- `<div>` заменён на `<label htmlFor="cb-...">` — браузер сам форвардит любой клик в связанный `<input>`
- Слушатель переведён с `click` на `change` — срабатывает ровно один раз при реальном переключении
- Убрана ручная инверсия `cb.checked = !cb.checked`
- Гард на снятие последней серии оставлен (`activeSeries.size <= 1`)
- `dropdown.addEventListener('click', e => e.stopPropagation())` оставлен — клики в дропдауне не закрывают его

Затронутые файлы:
- `frontend/debt-volume.html` (удалён дубликат + замена `KEYS` → `CHART_KEYS` + переписан `buildDropdown`)
- `frontend/debt-overdue.html` (замена `KEYS` → `CHART_KEYS` + переписан `buildDropdown`)

**TODO:** прогнать `grep -nE '\bKEYS\b' frontend/*.html` по всем 25 страницам с горизонтальной панелью — если копировался один и тот же шаблон, баг с двойным срабатыванием обработчика и/или с `KEYS` мог попасть и в другие файлы. Шаблон правильного `buildDropdown` теперь зафиксирован в debt-volume / debt-overdue.

---

## Что в работе ⏳

1. Горизонтальная панель — остальные файлы (subsidy-ddu-count/volume, apartments-*.html) — ждут файлов
2. Аудит дропдаунов серий на 25 комбо-страницах: проверить на (а) ссылки на `KEYS` без префикса, (б) старый кастомный click-обработчик с ручной инверсией `cb.checked`. Шаблон-эталон — в debt-volume / debt-overdue после v1.7
3. Расчётные индикаторы: 5.9, 5.10, 5.11, 5.12–5.15
4. Деплой на VPS

## Известные ограничения

- `1.1` Население: fedstat/31557 недоступен → данные захардкожены
- `2.13.ext` Доля ветхого жилья: данные только до 2015
- Парсеры domclick.py и rosreestr.py не реализованы
- Блок «Обновление данных» на главной: отложен до теста парсинга на проде

---

## Технические решения

| Решение | Причина |
|---------|---------|
| ECharts вместо Highcharts | Бесплатная лицензия Apache 2.0 |
| Vanilla HTML/JS без Node.js | Нет сборщика — деплой = копирование файлов |
| Backend синхронный (нет async) | Достаточно для начального трафика |
| Только абсолютные значения в data_points | YoY/MoM/YTD считает materialized view |
| Комбо-страницы как отдельные HTML | Не ломает API, не требует schema-изменений |
| fedstat POST dataGrid.do | Официальный API нестабильный и медленный |
| periodicity = 'quarterly' для 1.2, 1.3 | Данные выходят поквартально |
| COMBO_OVERRIDES в JS | Нет изменений в БД/API для навигационных правил |
| `safe_float_pct` для процента готовности | Старые файлы Матрицы проектов (до дек 2023) хранят `'80%'` как строку; новые — как число 0.8 |
| `sum_last_12_months` требует ровно 12 точек | Если данных не хватает — расчётный показатель не записывается, чтобы не искажать ряд |
| Запасы: знаменатель = ввод МЖС (2.3), не суммарный (2.1) | 2.1 включает ИЖС, методологически неверно для МЖД-строительства |
| Коэффициент поглощения: делитель = годовой объём продаж | Было деление на месячный объём (×12 завышение) — исправлено |
| Льготная ипотека 6.38/6.39 — `is_public=true`, нули→NULL | Исторические данные сохранены, пустые периоды не отображаются |
| Квартирография: статус из Матрицы проектов | Старые файлы без столбца статуса — джойн по ключу `Корпус` |
| Квартирография: интерполяция плохих периодов | BAD_PERIODS в migrate_apartments.py: сен.2023, дек.2023, янв.2024 |
| mm_budget в млн руб. | Удобочитаемость: значения 1–3 вместо 1 000 000–3 000 000 |
| Горизонтальная панель вместо сайдбара | Сайдбар убран с chart.html и всех комбо-страниц; панель с дропдауном серий только на комбо |
| Дропдаун серий: `<label>` + `change`-listener | Кастомный `click` на родителе ловил bubble-событие дважды → галочка визуально возвращалась. `<label htmlFor>` штатно форвардит клик в `<input>`, `change` срабатывает ровно один раз |
